# source code
src.lisp.dir=${project.dir}/lisp
src.java.dir=${project.dir}/java/src

# third party libraries
lib.java.dir=${project.dir}/java/lib

# create objects
build.dir=${project.dir}/build

build.config.dir=${build.dir}/config
config.build.file=${project.dir}/build.properties

build.lisp.dir=${build.dir}/lisp
build.lisp.src.file=${config.dir}/build.el
build.lisp.dst.file=${build.config.dir}/build.el
# for now, changes to this need to be reflected in the `require' in jde.el
build.lisp.autoload.libname=jde-autoload
build.lisp.autoload.file=${build.lisp.dir}/${build.lisp.autoload.libname}.el
build.java.dir=${build.dir}/classes

# staging 
dist.dir=${project.dir}/dist
dist.lisp.dir=${dist.dir}/lisp
dist.java.dir=${dist.dir}/java
dist.java.lib.dir=${dist.java.dir}/lib
dist.java.src.dir=${dist.java.dir}/src
dist.java.doc.dir=${dist.dir}/java/doc
dist.jar.jde.file=${dist.java.lib.dir}/jde.jar

dist.doc.dir=${dist.dir}/doc
dist.doc.java.api.dir=${dist.doc.dir}/java/api
dist.doc.html.dir=${dist.doc.dir}/html

